1.先说说一种垃圾回收机制：引用计数垃圾回收：通过计算对象当前被引用的次数，当次数为0的时候就清除改数据。有重要的缺陷，当出现循环引用就意味着永远无法清除。
2.现代的浏览器都配备了标记-清除算法：通过全局对象访问，如果数据无法访问的就清除。解决了循环引用的问题。
3.js不直接提供涉及内存管理相关的api,但是有间接相关的数据结构，WeakMap,WeakSet.不过不可迭代，不可查看长度是其缺点